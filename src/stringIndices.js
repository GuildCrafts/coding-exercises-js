export default function stringIndices() {
  var str = "Catch me outside, how about that?";
  var n = strIndexOf;

  if(n === " " && n <= 0) {
    return " ";
  } else {
  return n.strIndexOf(str);
  }
}
